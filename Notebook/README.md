The subtract function takes two numerical values as input, (a,b) and minuses the b variable from the a variable. It returns the output of this computation. Situations in which the function would not work are when non-valid inputs such as letters, complex numbers or any other non-numerical inputs are attempted. The function is defined with two potential vairables, and the name "subtract". It is used by running subtract(a,b) in a cell of python.