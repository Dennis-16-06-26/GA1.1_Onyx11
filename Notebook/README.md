Simple function made in python that can be used to make simple multiplications.
The function can be used when you want to multiply number a with b.
This can be done by print(multiply(a,b))
a and b can be there desired number, dont forget to run the first code before you run the second one. When ignoring this it can cause some problems